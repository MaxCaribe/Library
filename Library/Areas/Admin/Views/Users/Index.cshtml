@model BLL.ViewModels.UserListViewModel
@foreach (var p in Model.Users)
{
    <table>
        <thead>
            @p.
            @Html.Label("Name")
        </thead>
        <thead>
            @Html.Label("Name")
        </thead>
        <thead>
            @Html.Label("Name")
        </thead>
        <thead>
            @Html.Label("Name")
        </thead>
        <thead>
            @Html.Label("Name")
        </thead>
    </table>
}

